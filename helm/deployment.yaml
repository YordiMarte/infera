apiVersion: apps/v1
kind: Deployment
metadata:
  name: infera-gateway
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: infera
  template:
    metadata:
      labels:
        app: infera
    spec:
      containers:
        - name: gateway
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          ports:
            - containerPort: 3000